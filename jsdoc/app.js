/**
 * @description Shows a hello message to a given name.
 * @param {string} name - The name of the user.
@returns {string}
 */
var hello = function(name) {
	return 'Hello, ' + name;
};

console.log(hello('Daniel'));
